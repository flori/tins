dockerfile: |-
  RUN apk add --no-cache build-base yaml-dev git openssl-dev

script: &script |-
  echo -e "\e[1m"
  ruby -v
  echo -e "\e[0m"
  bundle install --jobs=$(getconf _NPROCESSORS_ONLN)
  bundle exec rake test RUBYOPT="--enable-frozen-string-literal --debug-frozen-string-literal"

before: |-
  echo 🏁 Preparing…
  echo Deleting Gemfile.lock
  rm -f Gemfile.lock

after: |-
  if [[ "$RESULT" = "1" ]]
  then
    echo 😵 Some tests failed!
  else
    echo 🥳 All tests passed!
  fi
  echo Deleting Gemfile.lock
  rm -f Gemfile.lock

fail_fast: true

images:
  ruby:4.0-alpine: *script
  ruby:3.4-alpine: *script
  ruby:3.3-alpine: *script
  ruby:3.2-alpine: *script
  ruby:3.1-alpine: *script
